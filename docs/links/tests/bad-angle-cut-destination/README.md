# Bad — L1, an angle destination cut short

The closing `>` is missing, so this is not the angle form and not a link on the
forge. It is kept whole and fails `L1` with the cut text shown; a reader that
took `<Design` for an adopter marker passed it in silence.

- A good link: [the target](target.md).
- The defect: [a cut destination](<Design Notes/target.md).
