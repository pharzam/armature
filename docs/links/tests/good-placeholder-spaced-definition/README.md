# Good — a reference definition whose target is a spaced adopter marker

A real link: [the target](target.md).

The runner is [the test runner][runner], a value only the adopter can fill. The
marker holds a blank, and that does not make the definition a defect.

[runner]: ‹the test runner›/run.sh
