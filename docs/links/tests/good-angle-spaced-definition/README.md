# Good — an angle destination with a space, reached through a reference definition

The only link here: [the target][t]. The definition carries a title, which is
what the angle form exists to allow: the `>` ends the destination, and the title
follows it. A reader that ignored the `>` would take the whole line as a marker.

[t]: <Design Notes/target.md> "The target"
