# Good — an angle destination with a space, followed by a title

The only link here: [the target](<Design Notes/target.md> "The target"). The
closing `>` is what lets the title follow; a reader that kept the whole text as
the destination would skip it as a marker, and resolve nothing.
