interface:
  display_name: "Armature"
  short_description: "Repo-specific patterns and workflows for armature"
  default_prompt: "Use the armature repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true