See [`AGENTS.md`](AGENTS.md).

That file is the agent context for this repository, and it is the only copy. This
file exists because some tools look for this name instead — it deliberately holds
a pointer and nothing else, so the two can never disagree.
